name: legacy_timeline_scrubber_example
description: "An example project for the legacy_timeline_scrubber."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  legacy_timeline_scrubber:
    path: ../
  provider: ^6.1.5+1

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true